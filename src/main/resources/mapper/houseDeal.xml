<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.house.model.repo.HouseDealRepo">
	<!-- <select id="search" resultType="HouseDealDto">
		select * from houseinfo i, housedeal d where i.no = d.no 
		<if test="key!='none'">
			and i.${key} like concat('%',#{word},'%');
		</if>
	</select> -->
</mapper>